import { ServerArgs } from 'src';
declare const _default: ({
  dbConnect,
  schema,
  config,
  routes,
  services,
  plugins,
  postRegisterHook,
  swaggerOptions,
  swaggerUiOptions,
  loggerOptions,
  serverOptions,
}: ServerArgs) => Promise<any>;
export default _default;
